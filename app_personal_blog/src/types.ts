export enum AppPages {
   LOGIN = "/login",
   REGISTRATION = "/registration",
   LANDING = "/landing",
   POSTS = "/posts",
   POSTS_ITEM = "/posts/:id",
   RESET_PASSWORD = "/reset-password",
   SUCCESS_PAGE = "/success-page",
 }